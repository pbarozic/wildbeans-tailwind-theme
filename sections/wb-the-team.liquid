{% comment %}
WILD BEANS — STORY + TEAM (Docu, Chapitré, Slides plein écran + Scroll Snap)
{% endcomment %}

<section
  id="wild-beans-story-team"
  class="w-screen h-screen overflow-y-scroll"
>
<nav class="wb-chapter-menu" aria-label="Chapters">
  <button class="wb-menu-item" data-scroll-to="#chapter-1" data-chapter="1">
    <span class="wb-menu-kicker">CHAPTER 01</span>
    <span class="wb-menu-title">The Intuition</span>
  </button>

  <button class="wb-menu-item" data-scroll-to="#chapter-2" data-chapter="2">
    <span class="wb-menu-kicker">CHAPTER 02</span>
    <span class="wb-menu-title">The Meeting</span>
  </button>

  <button class="wb-menu-item" data-scroll-to="#chapter-3" data-chapter="3">
    <span class="wb-menu-kicker">CHAPTER 03</span>
    <span class="wb-menu-title">On-Set Expertise</span>
  </button>

  <button class="wb-menu-item" data-scroll-to="#chapter-4" data-chapter="4">
    <span class="wb-menu-kicker">CHAPTER 04</span>
    <span class="wb-menu-title">The Future</span>
  </button>
</nav>


<section class="w-full sm:h-screen overflow-x-hidden snap-start relative js-events-video">
  <div class="w-full h-full barista-button">
    <video
      autoplay
      muted
      loop
      playsinline
      preload="auto"
      class="w-full h-full object-cover"
    >
      <source src="{{ 'V02.MOV' | file_url }}" type="video/mp4" />
      Votre navigateur ne supporte pas la lecture de vidéo.
    </video>
  </div>

  <!-- OVERLAY TEXTE ANIMÉ -->
  <div
    class="pointer-events-none absolute inset-0 flex flex-col items-center justify-center
           -translate-y-[8vh] px-4 text-center"
  >
    <h1
      id="hero-title"
      class="text-white font-bold
             text-[12vw] sm:text-[9vw] md:text-[7vw]
             leading-none tracking-tight
             drop-shadow-[0_0_25px_rgba(255,199,120,0.25)]
drop-shadow-[0_20px_55px_rgba(0,0,0,1)]
"
    ></h1>

    <p
      id="hero-subtitle"
      class="mt-4 text-white/80 text-4xl 
  tracking-[0.25em] uppercase
  drop-shadow-[0_0_25px_rgba(255,199,120,0.25)]
drop-shadow-[0_20px_55px_rgba(0,0,0,1)]"
"
    ></p>
  </div>
  <!-- CTA CHAPTER 1 -->
<button
  type="button"
  class="wb-chapter-btn wb-chapter-btn--hero"
  aria-label="Scroll to Chapter 1"
  data-scroll-to="#chapter-1"
>
  CHAPTER 1
</button>
</section>

  <!-- =============================== -->
  <!-- SLIDE 1 : OUR STORY + CHAPTER 01 -->
  <!-- =============================== -->

  <section  id="chapter-1" class="sm:w-1/2 h-screen flex items-center js-safe-layout relative">
    <div class=" px-4 space-y-6 md:space-y-8">

      <!-- CHAPTER 01 HEADER -->
      <div
        class="space-y-1 opacity-0 translate-y-6 will-change-transform
               transition-all duration-[3000ms] ease-[cubic-bezier(0.19,1,0.22,1)]"
        data-scroll-fade
        style="transition-delay:800ms;"
      >
        <p class="sm:text-2xl font-medium tracking-[0.25em] text-neutral-500">
          CHAPTER 01
        </p>
        <h3 class="sm:text-4xl font-semibold uppercase tracking-tight">
          The Intuition
        </h3>
      </div>

      <p
        class="text-base sm:text-lg md:text-xl leading-relaxed uppercase tracking-wide 
               opacity-0 translate-y-6 will-change-transform
               transition-all duration-[2500ms] ease-[cubic-bezier(0.19,1,0.22,1)]"
        data-scroll-fade
        style="transition-delay:1300ms;"
      >
        Wild Beans was born from a simple observation made by Vincent Rousseau on ad production sets:
        long days, tight schedules, and yet rarely a truly good cup of coffee. Before launching Wild Beans,
        Vincent had already founded 
        <a href="https://www.betterbewild.fr/paris" class="underline" target="_blank">
          Better Be Wild</a>,
        a Paris-based production company known for its ambitious creative direction and seamless on-set
        organization.
      </p>

      <!-- CH1 IMAGES -->
      <div class="max-w-[1400px] grid grid-cols-1 sm:grid-cols-2 gap-6 md:gap-8">

        <img
          src="{{ section.settings.story_img_1 | img_url: '1600x' }}"
          alt="Better Be Wild – Production"
          class="rounded-2xl object-cover h-80 sm:h-96 w-full shadow-lg opacity-0 translate-y-6
                 will-change-transform transition-all duration-[2100ms] ease-[cubic-bezier(0.19,1,0.22,1)]"
          data-scroll-fade
          style="transition-delay:1500ms;"
        />

        <img
          src="{{ section.settings.story_img_2 | img_url: '1600x' }}"
          alt="Behind the scenes – Vincent Rousseau"
          class="rounded-2xl object-cover h-80 sm:h-96 w-full shadow-lg opacity-0 translate-y-6
                 will-change-transform transition-all duration-[2100ms] ease-[cubic-bezier(0.19,1,0.22,1)]"
          data-scroll-fade
          style="transition-delay:1700ms;"
        />

      </div>

    </div>
  <button
  type="button"
  class="wb-chapter-btn wb-chapter-btn--dark"
  data-scroll-to="#chapter-2"
  aria-label="Scroll to Chapter 2"
>
  CHAPTER 2
</button>

  </section>


  <!-- =============================== -->
  <!-- SLIDE 2 : CHAPTER 02 -->
  <!-- =============================== -->

  <section id="chapter-2" class="sm:w-1/2 min-h-screen flex items-center">
  <div class="px-4 space-y-6 md:space-y-8">

    <!-- CHAPTER 02 HEADER -->
    <div
      class="space-y-1 opacity-0 translate-y-6 will-change-transform
             transition-all duration-[3000ms] ease-[cubic-bezier(0.19,1,0.22,1)]"
      data-scroll-fade
      style="transition-delay:800ms;"
    >
      <p class="sm:text-2xl font-medium tracking-[0.25em] text-neutral-500">
        CHAPTER 02
      </p>
      <h3 class="sm:text-4xl font-semibold uppercase tracking-tight">
        The Meeting
      </h3>
    </div>

    <p
      class="text-base sm:text-lg md:text-xl leading-relaxed uppercase tracking-wide
             opacity-0 translate-y-6 will-change-transform
             transition-all duration-[2500ms] ease-[cubic-bezier(0.19,1,0.22,1)]"
      data-scroll-fade
      style="transition-delay:1300ms;"
    >
      The project reached a new level when Vincent met Tom Darval, a barista with over 13 years in
      specialty coffee. Known for his mastery of sourcing, roasting profiles, extraction techniques,
      and coffee culture, Tom brought the technical depth Wild Beans needed. Beyond his work with
      Wild Beans, he is currently filming a documentary series with Arte.
    </p>

    <div class="max-w-[1400px] grid grid-cols-1 sm:grid-cols-2 gap-6 md:gap-8">

      <img
        src="{{ section.settings.tom_doc_img | img_url: '1600x' }}"
        alt="Tom Darval filming Arte documentary"
        class="rounded-2xl object-cover h-80 sm:h-96 w-full shadow-lg opacity-0 translate-y-6
               will-change-transform transition-all duration-[2100ms] ease-[cubic-bezier(0.19,1,0.22,1)]"
        data-scroll-fade
        style="transition-delay:1500ms;"
      />

      <img
        src="{{ section.settings.tom_doc_img2 | img_url: '1600x' }}"
        alt="On-set coffee service – Wild Beans"
        class="rounded-2xl object-cover h-80 sm:h-96 w-full shadow-lg opacity-0 translate-y-6
               will-change-transform transition-all duration-[2100ms] ease-[cubic-bezier(0.19,1,0.22,1)]"
        data-scroll-fade
        style="transition-delay:1700ms;"
      />

      <p
        class="text-xs sm:text-sm text-neutral-500 italic opacity-0 translate-y-6
               will-change-transform transition-all duration-[2100ms] ease-[cubic-bezier(0.19,1,0.22,1)] sm:col-span-2"
        data-scroll-fade
        style="transition-delay:1900ms;"
      >
        Tom filming his documentary series with Arte, capturing stories from the world of coffee.
      </p>

    </div>

  </div>

    <button
  type="button"
  class="wb-chapter-btn wb-chapter-btn--dark"
  data-scroll-to="#chapter-3"
  aria-label="Scroll to Chapter 3"
>
  CHAPTER 3
</button>
</section>



  <!-- =============================== -->
  <!-- SLIDE 3 : CHAPTER 03 -->
  <!-- =============================== -->

  <section id="chapter-3" class="sm:w-1/2 min-h-screen flex items-center">
  <div class="px-4 space-y-6 md:space-y-8">

    <!-- CHAPTER 03 HEADER -->
    <div
      class="space-y-1 opacity-0 translate-y-6 will-change-transform
             transition-all duration-[3000ms] ease-[cubic-bezier(0.19,1,0.22,1)]"
      data-scroll-fade
      style="transition-delay:800ms;"
    >
      <p class="sm:text-2xl font-medium tracking-[0.25em] text-neutral-500">
        CHAPTER 03
      </p>
      <h3 class="sm:text-4xl font-semibold uppercase tracking-tight">
        On-Set Expertise
      </h3>
    </div>

    <p
      class="text-base sm:text-lg md:text-xl leading-relaxed uppercase tracking-wide
             opacity-0 translate-y-6 will-change-transform
             transition-all duration-[2500ms] ease-[cubic-bezier(0.19,1,0.22,1)]"
      data-scroll-fade
      style="transition-delay:1300ms;"
    >
      With their complementary strengths : Vincent’s understanding of on-set realities and Tom’s specialty
      coffee expertise—Wild Beans evolved into a premium coffee service created specifically for shoots
      and production events.
    </p>

    <div class="max-w-[1400px] grid grid-cols-1 sm:grid-cols-2 gap-6 md:gap-8">

      <img
        src="{{ section.settings.chapter3_img_1 | img_url: '1600x' }}"
        alt="On-set coffee service – Wild Beans"
        class="rounded-2xl object-cover h-80 sm:h-96 w-full shadow-lg opacity-0 translate-y-6
               will-change-transform transition-all duration-[2100ms] ease-[cubic-bezier(0.19,1,0.22,1)]"
        data-scroll-fade
        style="transition-delay:1500ms;"
      />

      <img
        src="{{ section.settings.chapter3_img_2 | img_url: '1600x' }}"
        alt="Barista station – Wild Beans"
        class="rounded-2xl object-cover h-80 sm:h-96 w-full shadow-lg opacity-0 translate-y-6
               will-change-transform transition-all duration-[2100ms] ease-[cubic-bezier(0.19,1,0.22,1)]"
        data-scroll-fade
        style="transition-delay:1700ms;"
      />

    </div>

  </div>

   <button
  type="button"
  class="wb-chapter-btn wb-chapter-btn--dark"
  data-scroll-to="#chapter-4"
  aria-label="Scroll to Chapter 4"
>
  CHAPTER 4
</button>
</section>




  <!-- =============================== -->
  <!-- SLIDE 4 : CHAPTER 04 + LINKS -->
  <!-- =============================== -->

  <section  id="chapter-4" class="sm:w-1/2 min-h-screen flex items-center">
  <div class="px-4 space-y-6 md:space-y-8">

    <!-- CHAPTER 04 HEADER -->
    <div
      class="space-y-1 opacity-0 translate-y-6 will-change-transform
             transition-all duration-[3000ms] ease-[cubic-bezier(0.19,1,0.22,1)]"
      data-scroll-fade
      style="transition-delay:800ms;"
    >
      <p class="sm:text-2xl font-medium tracking-[0.25em] text-neutral-500">
        CHAPTER 04
      </p>
      <h3 class="sm:text-4xl font-semibold uppercase tracking-tight">
        The Future
      </h3>
    </div>

    <p
      class="text-base sm:text-lg md:text-xl leading-relaxed uppercase tracking-wide
             opacity-0 translate-y-6 will-change-transform
             transition-all duration-[2500ms] ease-[cubic-bezier(0.19,1,0.22,1)]"
      data-scroll-fade
      style="transition-delay:1300ms;"
    >
      Looking ahead, Wild Beans is expanding through two major projects: our own coffee line, and BEANS,
      a media project and YouTube channel exploring global coffee culture.
    </p>

    <div class="max-w-[1400px] grid grid-cols-1 sm:grid-cols-2 gap-6 md:gap-8">

      <a href="/products" class="group block space-y-2">
        <img
          src="{{ section.settings.brand_img | img_url: '1600x' }}"
          alt="Wild Beans Coffee – Our Products"
          class="rounded-2xl object-cover h-80 sm:h-96 w-full shadow-lg opacity-0 translate-y-6
                 will-change-transform transition-all duration-[2100ms] ease-[cubic-bezier(0.19,1,0.22,1)]
                 group-hover:opacity-90"
          data-scroll-fade
          style="transition-delay:1500ms;"
        />
        <p
          class="text-sm uppercase tracking-wide opacity-0 translate-y-6
                 will-change-transform transition-all duration-[2100ms] ease-[cubic-bezier(0.19,1,0.22,1)]"
          data-scroll-fade
          style="transition-delay:1700ms;"
        >
          Discover Wild Beans Coffee →
        </p>
      </a>

      <a href="https://www.youtube.com/@wildbeans" target="_blank" class="group block space-y-2">
        <img
          src="{{ section.settings.youtube_img | img_url: '1600x' }}"
          alt="Beans – YouTube Channel"
          class="rounded-2xl object-cover h-80 sm:h-96 w-full shadow-lg opacity-0 translate-y-6
                 will-change-transform transition-all duration-[2100ms] ease-[cubic-bezier(0.19,1,0.22,1)]
                 group-hover:opacity-90"
          data-scroll-fade
          style="transition-delay:1600ms;"
        />
        <p
          class="text-sm uppercase tracking-wide opacity-0 translate-y-6
                 will-change-transform transition-all duration-[2100ms] ease-[cubic-bezier(0.19,1,0.22,1)]"
          data-scroll-fade
          style="transition-delay:1800ms;"
        >
          Explore Beans on YouTube →
        </p>
      </a>

    </div>

  </div>
</section>



  <!-- =============================== -->
  <!-- SLIDE 5 : TEAM SECTION -->
  <!-- =============================== -->

  <section class="sm:w-1/2 min-h-screen flex items-center">
    <div class="w-full max-w-[1400px] px-4 space-y-8 md:space-y-10">

      <div
        class="space-y-2 opacity-0 translate-y-6 will-change-transform
               transition-all duration-[1900ms] ease-[cubic-bezier(0.19,1,0.22,1)]"
        data-scroll-fade
        style="transition-delay:300ms;"
      >
        <p class="text-[11px] sm:text-xs font-medium tracking-[0.25em] text-neutral-400">
          CAST & CREW
        </p>

        <h2 class="text-3xl sm:text-4xl md:text-5xl font-bold uppercase tracking-tight">
          The Wild Beans Team
        </h2>
      </div>

      <div class="grid gap-6 md:gap-8 md:grid-cols-3">

        <!-- VINCENT -->
        <article
          class="group flex flex-col items-start text-left rounded-2xl border border-neutral-200/60 
                 bg-white/70 backdrop-blur-sm p-8 sm:p-10 shadow-sm hover:shadow-xl hover:-translate-y-2 
                 transition-all duration-300 ease-out opacity-0 translate-y-6 will-change-transform"
          data-scroll-fade
          style="transition-delay:600ms;"
        >
          <img
            src="{{ section.settings.vincent_image | img_url: '600x' }}"
            alt="Portrait of Vincent Rousseau"
            class="h-56 w-56 object-cover rounded-2xl shadow-md ring-2 ring-neutral-100 mb-6"
          />

          <h3 class="text-xl sm:text-2xl font-semibold uppercase">Vincent Rousseau</h3>
          <p class="text-xs sm:text-sm uppercase tracking-wide text-neutral-500 mb-4">
            Founder &amp; Production Lead
          </p>
          <p class="text-sm sm:text-base uppercase leading-relaxed text-neutral-700">
            Vincent leads operations, planning, logistics, and on-set coordination.
          </p>
        </article>

        <!-- TOM -->
        <article
          class="group flex flex-col items-start text-left rounded-2xl border border-neutral-200/60 
                 bg-white/70 backdrop-blur-sm p-8 sm:p-10 shadow-sm hover:shadow-xl hover:-translate-y-2 
                 transition-all duration-300 ease-out opacity-0 translate-y-6 will-change-transform"
          data-scroll-fade
          style="transition-delay:750ms;"
        >
          <img
            src="{{ section.settings.tom_image | img_url: '600x' }}"
            alt="Portrait of Tom Darval"
            class="h-56 w-56 object-cover rounded-2xl shadow-md ring-2 ring-neutral-100 mb-6"
          />

          <h3 class="text-xl sm:text-2xl font-semibold uppercase">Tom Darval</h3>
          <p class="text-xs sm:text-sm uppercase tracking-wide text-neutral-500 mb-4">
            Head of Coffee &amp; Experience
          </p>
          <p class="text-sm sm:text-base uppercase leading-relaxed text-neutral-700">
            Tom defines the coffee direction, bean selection, training, and standards.
          </p>
        </article>

        <!-- PB -->
        <article
          class="group flex flex-col items-start text-left rounded-2xl border border-neutral-200/60 
                 bg-white/70 backdrop-blur-sm p-8 sm:p-10 shadow-sm hover:shadow-xl hover:-translate-y-2 
                 transition-all duration-300 ease-out opacity-0 translate-y-6 will-change-transform"
          data-scroll-fade
          style="transition-delay:900ms;"
        >
          <img
            src="{{ section.settings.pb_image | img_url: '600x' }}"
            alt="Portrait of Pierre-Baptiste Rozic"
            class="h-56 w-56 object-cover rounded-2xl shadow-md ring-2 ring-neutral-100 mb-6"
          />

          <h3 class="text-xl sm:text-2xl font-semibold uppercase">Pierre-Baptiste Rozic</h3>
          <p class="text-xs sm:text-sm uppercase tracking-wide text-neutral-500 mb-4">
            Barista &amp; Logistics Support
          </p>
          <p class="text-sm sm:text-base uppercase leading-relaxed text-neutral-700">
            Supports coffee service, logistics, and setup throughout the production day.
          </p>
        </article>

      </div>

    </div>
  </section>

</section>
<style>
  .wb-letter {
    opacity: 0;
    transform: translateY(14px);
    display: inline-block;
    animation: wbFadeUp 0.9s forwards ease-out;
  }

  @keyframes wbFadeUp {
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

/* BASE */
/* BASE (PAS d'animation ici) */
.wb-chapter-btn{
  z-index: 999;
  pointer-events: auto;
  font-size: 14px;
  letter-spacing: 0.28em;
  text-transform: uppercase;
}

/* HERO (vidéo) */
.wb-chapter-btn--hero{
  position: absolute;
  left: 50%;
  bottom: 28px;
  transform: translateX(-50%);
  color: white;

  /* blink uniquement ici */
  animation: wbBlink 1.4s infinite ease-in-out;
}

/* DARK (chapters) : caché par défaut */
.wb-chapter-btn--dark{
  position: fixed;
  left: 50%;
  bottom: 28px;
  transform: translateX(-50%);

  color: #111;

  padding: 10px 14px;
  border-radius: 999px;

  opacity: 0;
  pointer-events: none;
  transition: opacity 500ms cubic-bezier(0.19,1,0.22,1);

  /* surtout : pas d'animation tant que pas actif */
  animation: none;
}

/* quand actif : visible + blink */
.wb-chapter-btn--dark.is-active{
  opacity: 1;
  pointer-events: auto;

  animation: wbBlink 1.4s infinite ease-in-out;
}

@keyframes wbBlink{
  0%, 100% { opacity: 0.25; transform: translateX(-50%) translateY(0); }
  50%      { opacity: 1;    transform: translateX(-50%) translateY(-4px); }
}

/* =========================
   RIGHT CHAPTER MENU
========================= */
.wb-chapter-menu{
  position: fixed;
  right: 18px;
  top: 30%;
  z-index: 1200;

  display: flex;
  flex-direction: column;
  gap: 10px;
  pointer-events: auto;
}

/* bouton du menu */
.wb-menu-item{
  text-align: left;
  background: transparent;
  padding: 6px 10px;
  cursor: pointer;

  opacity: 0;
  transform: translateX(0);
  transition: opacity 500ms cubic-bezier(0.19,1,0.22,1),
              transform 700ms cubic-bezier(0.19,1,0.22,1);
}

/* petit "CHAPTER 01" */
.wb-menu-kicker{
  display:block;
  font-size: 10px;
  letter-spacing: 0.28em;
  text-transform: uppercase;
  color: rgba(17,17,17,0.55);
}

/* titre */
.wb-menu-title{
  display:block;
  font-size: 13px;
  letter-spacing: 0.08em;
  text-transform: uppercase;
  color: rgba(17,17,17,0.8);
}

/* actif = plus visible + un peu indent */
.wb-menu-item.is-active{
  opacity: 1}

/* "découvert" = indentation progressive */
.wb-menu-item.is-seen{
  opacity: 0.7;
}

/* option mobile: tu peux masquer si tu veux */
@media (max-width: 640px){
  .wb-chapter-menu{ display:none; }
}

</style>

<script>
document.addEventListener("DOMContentLoaded", function () {

  /* ======================================================
     HERO — animation du texte
  ====================================================== */
  var titleText = "DISCOVER OUR STORY";
  var subtitleText = "A journey shaped by coffee, moments and people";
  var titleEl = document.getElementById("hero-title");
  var subtitleEl = document.getElementById("hero-subtitle");

  function animateText(el, text, baseDelay) {
    if (!el) return;
    el.innerHTML = "";
    Array.from(text).forEach(function (char, index) {
      var span = document.createElement("span");
      span.className = "wb-letter";
      span.textContent = char === " " ? "\u00A0" : char;
      span.style.animationDelay = (baseDelay + index * 0.08) + "s";
      el.appendChild(span);
    });
  }

  animateText(titleEl, titleText, 0.08);
  animateText(subtitleEl, subtitleText, 0.8);

  /* ======================================================
     CONTAINER SCROLL
  ====================================================== */
  var slider = document.getElementById("wild-beans-story-team");
  if (!slider) return;

  /* ======================================================
     FADE-IN AU SCROLL
  ====================================================== */
  var fadeEls = document.querySelectorAll("[data-scroll-fade]");

  if (!("IntersectionObserver" in window)) {
    fadeEls.forEach(function (el) {
      el.classList.remove("opacity-0", "translate-y-6");
    });
    return;
  }

  var fadeObserver = new IntersectionObserver(function (entries) {
    entries.forEach(function (entry) {
      if (!entry.isIntersecting) return;
      entry.target.classList.remove("opacity-0", "translate-y-6");
      entry.target.classList.add("opacity-100", "translate-y-0");
      fadeObserver.unobserve(entry.target);
    });
  }, {
    root: slider,
    threshold: 0.2
  });

  fadeEls.forEach(function (el) {
    fadeObserver.observe(el);
  });

  /* ======================================================
     NAVIGATION (boutons + menu) via data-scroll-to
  ====================================================== */
  var map = {
    "#chapter-1": 1,
    "#chapter-2": 2,
    "#chapter-3": 3,
    "#chapter-4": 4,
    "#team": 5
  };

  document.querySelectorAll("[data-scroll-to]").forEach(function (btn) {
    btn.addEventListener("click", function (e) {
      e.preventDefault();
      if (typeof slider._goToSlide !== "function") return;

      var target = btn.getAttribute("data-scroll-to");
      if (map[target] !== undefined) slider._goToSlide(map[target]);
    });
  });

  /* ======================================================
     CHAPTER "NEXT" dark buttons : 1 seul visible + delay
  ====================================================== */
  var darkButtons = Array.from(document.querySelectorAll(".wb-chapter-btn--dark"));
  var buttonDelay = 800; // ms
  var buttonTimeout = null;

  function setActiveDarkButton(btn) {
    darkButtons.forEach(function (b) {
      b.classList.remove("is-active");
    });

    if (buttonTimeout) {
      clearTimeout(buttonTimeout);
      buttonTimeout = null;
    }

    if (!btn) return;

    buttonTimeout = setTimeout(function () {
      btn.classList.add("is-active");
    }, buttonDelay);
  }

  setActiveDarkButton(null);

  /* ======================================================
     RIGHT MENU: active + indentation progressive ("discover")
  ====================================================== */
  var menuItems = Array.from(document.querySelectorAll(".wb-menu-item"));
  var seenMax = 0; // 0..4

  function setActiveMenu(chapterIndex) {
    menuItems.forEach(function (item) {
      item.classList.remove("is-active");
    });

    var active = menuItems[chapterIndex - 1];
    if (active) active.classList.add("is-active");
  }

  function setSeenUpTo(chapterIndex) {
    if (chapterIndex <= seenMax) return;
    seenMax = chapterIndex;

    menuItems.forEach(function (item, idx) {
      var i = idx + 1;

      // on marque "seen"
      if (i <= seenMax) item.classList.add("is-seen");

      // indentation progressive (plus tu avances, plus ça “pousse”)
      // tu peux ajuster 6 -> 8 si tu veux plus marqué
      var indent = Math.max(0, i - 1) * 6; 
      item.style.transform = "translateX(" + (-indent) + "px)";
    });

    // l'actif aura encore un petit "extra" via .is-active (CSS)
    setActiveMenu(chapterIndex);
  }

  /* ======================================================
     OBSERVER DES CHAPITRES (pour bouton dark + menu)
  ====================================================== */
  var chapterSelectors = ["#chapter-1", "#chapter-2", "#chapter-3", "#chapter-4"];
  var chapterEls = chapterSelectors
    .map(function (sel) { return document.querySelector(sel); })
    .filter(Boolean);

  var chapterObserver = new IntersectionObserver(function (entries) {
    var visibleEntries = entries
      .filter(function (e) { return e.isIntersecting; })
      .sort(function (a, b) { return b.intersectionRatio - a.intersectionRatio; });

    var visible = visibleEntries[0];
    if (!visible) return;

    // déterminer l'index chapitre (1..4)
    var id = "#" + visible.target.id;
    var chapterIndex = chapterSelectors.indexOf(id) + 1;

    // bouton dark dans la section visible
    var btn = visible.target.querySelector(".wb-chapter-btn--dark");
    setActiveDarkButton(btn);

    // menu: actif + "discover"
    if (chapterIndex > 0) setSeenUpTo(chapterIndex);

  }, {
    root: slider,
    threshold: [0.35, 0.5, 0.65]
  });

  chapterEls.forEach(function (el) {
    chapterObserver.observe(el);
  });

});
</script>




{% schema %}
{
  "name": "Wild Beans – Story",
  "settings": [
    { "type": "image_picker", "id": "story_img_1", "label": "Story image 1" },
    { "type": "image_picker", "id": "story_img_2", "label": "Story image 2" },
    { "type": "image_picker", "id": "tom_doc_img", "label": "Tom documentary image" },
    { "type": "image_picker", "id": "tom_doc_img2", "label": "Tom documentary image" },
    { "type": "image_picker", "id": "brand_img", "label": "Brand image" },
    { "type": "image_picker", "id": "youtube_img", "label": "YouTube image" },
    { "type": "image_picker", "id": "vincent_image", "label": "Vincent image" },
    { "type": "image_picker", "id": "tom_image", "label": "Tom image" },
    { "type": "image_picker", "id": "pb_image", "label": "PB image" },
    { "type": "image_picker", "id": "chapter3_img_1", "label": "Chapter 3 image 1" },
    { "type": "image_picker", "id": "chapter3_img_2", "label": "Chapter 3 image 2" }
  ],
  "presets": [
    { "name": "Wild Beans – Story + Team (Editorial Left, Slides)" }
  ]
}
{% endschema %}
